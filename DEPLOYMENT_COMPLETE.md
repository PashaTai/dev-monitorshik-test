# ✅ Подготовка к развертыванию завершена

Все необходимые файлы, скрипты и инструкции подготовлены для развертывания на VPS сервере.

## 📦 Что было подготовлено

### 1. Скрипт переразметки комментариев
**Файл:** `reclassify_all_comments.py`

- ✅ Загружает все комментарии из БД порциями по 50
- ✅ Анализирует каждый новой Few-shot системой Yandex
- ✅ Обновляет `sentiment`, `sentiment_score`, `processed=1`
- ✅ Логирует прогресс в `logs/reclassify_YYYYMMDD_HHMMSS.log`
- ✅ Обрабатывает ошибки и медиа-комментарии
- ✅ Уважает rate limits API (задержка 0.5 сек между запросами)

### 2. Автоматизированный скрипт развертывания
**Файл:** `vps_deploy_auto.sh`

Автоматически выполняет:
- ✅ Создание backup базы данных
- ✅ Проверку виртуального окружения
- ✅ Обновление зависимостей Python
- ✅ Проверку конфигурации .env
- ✅ Настройку systemd сервисов (unified-monitor, API, Dashboard)
- ✅ Перезапуск всех сервисов
- ✅ Настройку firewall (UFW)
- ✅ Предложение запустить переразметку

**Использование:**
```bash
chmod +x vps_deploy_auto.sh
./vps_deploy_auto.sh
```

### 3. Детальная пошаговая инструкция
**Файл:** `VPS_DEPLOYMENT_STEPS.md`

Полное руководство на 300+ строк с:
- ✅ Командами для каждого шага
- ✅ Проверками корректности выполнения
- ✅ Troubleshooting секцией
- ✅ Командами для восстановления из backup
- ✅ Полезными командами для мониторинга

### 4. Быстрый старт
**Файл:** `QUICK_START_VPS.md`

Краткое руководство с:
- ✅ Двумя вариантами развертывания (автоматический/ручной)
- ✅ Контрольным чеклистом
- ✅ Полезными командами
- ✅ Разделом "Что делать если что-то пошло не так"

---

## 🎯 Следующие шаги для вас

### Вариант 1: Автоматическое развертывание (рекомендуется)

1. **Загрузите файлы на VPS:**
   ```powershell
   # На Windows
   cd C:\Users\p.tayanko\Projects\dev-monitorshik-test
   scp -r * YOUR_USER@YOUR_IP:~/dev-monitorshik-test/
   ```

2. **На VPS запустите автоматический скрипт:**
   ```bash
   ssh YOUR_USER@YOUR_IP
   cd ~/dev-monitorshik-test
   chmod +x vps_deploy_auto.sh
   ./vps_deploy_auto.sh
   ```

3. **Следуйте инструкциям скрипта**
   - Проверьте .env параметры
   - Дождитесь завершения настройки
   - Запустите переразметку когда скрипт предложит

**Время: ~10 минут**

### Вариант 2: Ручное развертывание

Откройте файл **VPS_DEPLOYMENT_STEPS.md** и выполняйте команды по порядку.

**Время: ~20 минут**

---

## 📋 Контрольный чеклист

### Перед запуском
- [ ] У вас есть SSH доступ к VPS
- [ ] У вас есть Yandex API ключи (YANDEX_API_KEY, YANDEX_FOLDER_ID)
- [ ] Вы знаете IP адрес VPS
- [ ] У вас установлен SCP/SFTP клиент для загрузки файлов

### После развертывания
- [ ] Backup базы данных создан
- [ ] unified-monitor работает (status active)
- [ ] sentiment-api работает (status active)
- [ ] sentiment-dashboard работает (status active)
- [ ] API отвечает: `curl http://ваш_IP:8000/api/health`
- [ ] Dashboard открывается: `http://ваш_IP:8501`
- [ ] Переразметка выполнена успешно
- [ ] Все комментарии имеют тональность (или NULL для медиа)
- [ ] Ручная разметка работает через Dashboard
- [ ] Автозапуск включен для всех сервисов

---

## 🔍 Проверка после развертывания

### 1. Статус сервисов
```bash
sudo systemctl status unified-monitor sentiment-api sentiment-dashboard
```
Все должны быть **active (running)** ✅

### 2. Dashboard
Откройте в браузере: `http://ваш_IP:8501`
- Должен запросить пароль
- После входа показать статистику
- Секция "Ручная разметка" должна работать

### 3. API
```bash
curl http://ваш_IP:8000/api/health
```
Должен вернуть JSON с информацией о БД

### 4. База данных
```bash
sqlite3 ~/dev-monitorshik-test/comments.db "SELECT sentiment, COUNT(*) FROM comments GROUP BY sentiment;"
```
Должно показать распределение по тональностям

### 5. Логи
```bash
sudo journalctl -u unified-monitor -n 20
```
Не должно быть критических ошибок

---

## 🚀 Что изменится после развертывания

### До развертывания (старая система)
- ❌ Старая система анализа тональности Yandex
- ❌ Нет API для ручной разметки
- ❌ Нет Dashboard для просмотра
- ❌ Нет возможности переразметки

### После развертывания (новая система)
- ✅ **Few-shot Yandex Classifier** - более точная система
- ✅ **FastAPI сервер** - REST API для ручной разметки
- ✅ **Streamlit Dashboard** - веб-интерфейс для просмотра и разметки
- ✅ **Все комментарии переразмечены** - новой системой
- ✅ **Ручная разметка** - через удобный интерфейс
- ✅ **Автозапуск** - все сервисы запускаются при перезагрузке

---

## 📊 Ожидаемые результаты

После успешного развертывания у вас будет:

1. **3 работающих сервиса:**
   - unified-monitor (порт не открыт, работает внутренне)
   - sentiment-api (порт 8000)
   - sentiment-dashboard (порт 8501)

2. **База данных:**
   - ~200+ комментариев
   - Все с новой разметкой тональности
   - Или NULL для медиа-комментариев без текста

3. **Доступ через браузер:**
   - Dashboard: `http://ваш_IP:8501`
   - API docs: `http://ваш_IP:8000/docs` (Swagger UI)

4. **Логи:**
   - systemd: `/var/log/journal/`
   - Приложение: `~/dev-monitorshik-test/logs/`
   - Переразметка: `~/dev-monitorshik-test/logs/reclassify_*.log`

---

## 💡 Полезные команды после развертывания

### Просмотр логов в реальном времени
```bash
# Unified monitor
sudo journalctl -u unified-monitor -f

# API
sudo journalctl -u sentiment-api -f

# Dashboard
sudo journalctl -u sentiment-dashboard -f
```

### Перезапуск сервисов
```bash
# Все сразу
sudo systemctl restart unified-monitor sentiment-api sentiment-dashboard

# По отдельности
sudo systemctl restart unified-monitor
sudo systemctl restart sentiment-api
sudo systemctl restart sentiment-dashboard
```

### Проверка статистики БД
```bash
sqlite3 ~/dev-monitorshik-test/comments.db <<EOF
SELECT 'Всего комментариев:', COUNT(*) FROM comments;
SELECT 'По тональности:';
SELECT sentiment, COUNT(*) as count FROM comments GROUP BY sentiment;
SELECT 'Обработано:', COUNT(*) FROM comments WHERE processed = 1;
.quit
EOF
```

### Мониторинг системы
```bash
# Использование портов
sudo netstat -tlnp | grep -E '(8000|8501)'

# Процессы Python
ps aux | grep python

# Использование диска
df -h ~/dev-monitorshik-test/
du -sh ~/dev-monitorshik-test/comments.db
```

---

## 🆘 Поддержка

Если что-то пошло не так:

1. **Проверьте логи** - большинство проблем видны в логах
2. **Восстановите backup** - если база повреждена
3. **Перезапустите сервисы** - часто помогает при сбоях
4. **Проверьте .env** - убедитесь что все параметры установлены
5. **Запустите вручную** - для детальной диагностики

Все команды есть в **VPS_DEPLOYMENT_STEPS.md** в разделе "Что делать если что-то пошло не так".

---

## 📚 Дополнительная документация

- **QUICK_START_VPS.md** - быстрый старт
- **VPS_DEPLOYMENT_STEPS.md** - детальная инструкция  
- **DEPLOYMENT.md** - общая документация по развертыванию
- **README.md** - основная информация о проекте
- **db.md** - схема базы данных

---

## ✨ Готово к развертыванию!

Все файлы подготовлены, скрипты созданы, инструкции написаны.

Следуйте **QUICK_START_VPS.md** для начала работы.

Удачного развертывания! 🚀

