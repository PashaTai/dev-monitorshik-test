# ✅ Подготовка завершена - Git-based развертывание

Все готово для развертывания через Git на VPS сервере.

## 📦 Что подготовлено

### 1. Код и скрипты
- ✅ **reclassify_all_comments.py** - переразметка всех 200+ комментариев
- ✅ **sentiment/yandex_analyzer.py** - новая Few-shot система Yandex
- ✅ **api_server.py** - FastAPI для ручной разметки
- ✅ **dashboard/streamlit_app.py** - веб-интерфейс с разметкой
- ✅ **deploy/*.service** - systemd сервисы для автозапуска

### 2. Конфигурация
- ✅ **.gitignore** - исключает БД, логи, `.env` из git
- ✅ **requirements.txt** - все зависимости
- ✅ **env.example** - пример конфигурации

### 3. Документация
- ✅ **GIT_DEPLOYMENT.md** - Git workflow для развертывания
- ✅ **START_HERE.md** - главная точка входа
- ✅ **QUICK_START_VPS.md** - быстрый старт
- ✅ **VPS_DEPLOYMENT_STEPS.md** - детальные команды

---

## 🚀 Как развернуть

### Шаг 1: Commit и Push (Windows)

```powershell
cd C:\Users\p.tayanko\Projects\dev-monitorshik-test
git add .
git commit -m "Добавлена Few-shot система тональности"
git push origin main
```

### Шаг 2: Pull и Update (VPS)

```bash
ssh YOUR_USER@YOUR_IP
cd ~/dev-monitorshik-test

# Backup БД
cp comments.db comments.db.backup.$(date +%Y%m%d_%H%M%S)

# Получить изменения
git pull origin main

# Обновить зависимости
source venv/bin/activate
pip install -r requirements.txt

# Перезапустить сервисы
sudo systemctl restart unified-monitor sentiment-api sentiment-dashboard
```

### Шаг 3: Переразметка (VPS)

```bash
screen -S reclassify
cd ~/dev-monitorshik-test
source venv/bin/activate
python reclassify_all_comments.py
# Ctrl+A, D для отсоединения
```

---

## 📋 Checklist развертывания

### Перед началом
- [ ] Git репозиторий настроен и доступен
- [ ] У вас есть SSH доступ к VPS
- [ ] `.env` файл на VPS обновлен с API ключами
- [ ] Yandex API ключи получены

### На локальной машине
- [ ] Проверили изменения: `git status`
- [ ] Добавили файлы: `git add .`
- [ ] Сделали commit: `git commit -m "..."`
- [ ] Push в репозиторий: `git push origin main`

### На VPS
- [ ] Создали backup БД
- [ ] Получили изменения: `git pull origin main`
- [ ] Обновили зависимости: `pip install -r requirements.txt`
- [ ] Перезапустили сервисы: `systemctl restart ...`
- [ ] Проверили статус сервисов: `systemctl status ...`
- [ ] Запустили переразметку: `python reclassify_all_comments.py`

### Проверка результата
- [ ] Dashboard открывается: `http://IP:8501`
- [ ] API отвечает: `curl http://IP:8000/api/health`
- [ ] Unified-monitor работает: `systemctl status unified-monitor`
- [ ] Переразметка завершена успешно
- [ ] Все комментарии имеют тональность

---

## 🔍 Что изменилось

### Старая система (до)
- ❌ Старая система анализа Yandex
- ❌ Нет API для ручной разметки
- ❌ Нет Dashboard
- ❌ Нет инструментов переразметки

### Новая система (после)
- ✅ **Few-shot Yandex Classifier** - точнее
- ✅ **FastAPI** - REST API на порту 8000
- ✅ **Streamlit Dashboard** - веб-интерфейс на порту 8501
- ✅ **Ручная разметка** - через удобный UI
- ✅ **Скрипт переразметки** - для всех комментариев
- ✅ **Git-based deployment** - через `git pull`

---

## 💡 Git Workflow

### Регулярные обновления

```bash
# Локально
git add .
git commit -m "Описание изменений"
git push

# На VPS
git pull
sudo systemctl restart unified-monitor sentiment-api sentiment-dashboard
```

### Откат изменений

```bash
# Если что-то пошло не так
git log --oneline  # Посмотреть историю
git checkout <commit-hash>  # Вернуться к версии
sudo systemctl restart unified-monitor sentiment-api sentiment-dashboard
```

---

## 📊 Что НЕ попадает в Git

Благодаря `.gitignore`:
- ❌ `comments.db` - база данных (остается только на VPS)
- ❌ `.env` - секреты и пароли
- ❌ `logs/` - логи
- ❌ `venv/` - виртуальное окружение
- ❌ `__pycache__/` - Python cache
- ❌ `*.backup.*` - backup файлы

---

## 🎯 Результат

После развертывания у вас будет:

1. ✅ **3 работающих сервиса:**
   - `unified-monitor` - мониторинг + анализ тональности
   - `sentiment-api` - REST API (порт 8000)
   - `sentiment-dashboard` - Dashboard (порт 8501)

2. ✅ **База данных:**
   - ~200+ комментариев переразмечены новой системой

3. ✅ **Доступ:**
   - Dashboard: `http://ваш_IP:8501`
   - API: `http://ваш_IP:8000/api/health`
   - Swagger: `http://ваш_IP:8000/docs`

4. ✅ **Git workflow:**
   - Локально: `git push`
   - На VPS: `git pull` + restart

---

## 📚 Дополнительные ресурсы

- **GIT_DEPLOYMENT.md** - детальный Git workflow
- **START_HERE.md** - главная инструкция
- **QUICK_START_VPS.md** - быстрый старт
- **VPS_DEPLOYMENT_STEPS.md** - команды для каждого шага

---

## ✨ Готово к развертыванию!

Откройте **GIT_DEPLOYMENT.md** или **START_HERE.md** и начните развертывание.

**Удачи! 🚀**

