# 🚀 НАЧНИТЕ ОТСЮДА - Развертывание на VPS

## ✅ Всё готово к развертыванию!

Для вас подготовлены все необходимые файлы, скрипты и инструкции для развертывания новой системы анализа тональности на VPS сервере.

---

## 📁 Подготовленные файлы

| Файл | Описание |
|------|----------|
| **reclassify_all_comments.py** | Скрипт для переразметки всех комментариев новой Few-shot системой |
| **.gitignore** | Исключает БД, логи и секреты из git |
| **QUICK_START_VPS.md** | ⭐ Краткое руководство для быстрого старта |
| **VPS_DEPLOYMENT_STEPS.md** | Детальная пошаговая инструкция с командами |
| **DEPLOYMENT_COMPLETE.md** | Информация о подготовленных материалах |

---

## 🎯 Что делать дальше

### Используем Git для развертывания

Все изменения разворачиваются через `git pull` на VPS сервере

---

## 💡 Рекомендуемый порядок действий

### 1. Подготовка (на локальной машине)

Сделайте commit и push изменений:

```powershell
# Windows PowerShell
cd C:\Users\p.tayanko\Projects\dev-monitorshik-test

# Проверьте статус
git status

# Добавьте новые файлы
git add .

# Сделайте commit
git commit -m "Добавлена новая система тональности Few-shot и инструменты развертывания"

# Push в репозиторий
git push origin main
```

### 2. Обновление кода на VPS

```bash
# Подключитесь к VPS
ssh YOUR_USER@YOUR_IP

# Перейдите в директорию проекта
cd ~/dev-monitorshik-test

# Получите обновления
git pull origin main
```

**Замените:**
- `YOUR_USER` - ваше имя пользователя на VPS
- `YOUR_IP` - IP адрес вашего VPS

### 3. Обновление .env на VPS

```bash
# Подключитесь к VPS
ssh YOUR_USER@YOUR_IP

# Отредактируйте .env
cd ~/dev-monitorshik-test
nano .env
```

**Добавьте/проверьте эти параметры:**

```env
# Yandex API (ОБЯЗАТЕЛЬНО!)
YANDEX_API_KEY=ваш_api_key
YANDEX_FOLDER_ID=ваш_folder_id

# Пароли (ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ!)
API_PASSWORD=надежный_пароль_123
DASHBOARD_PASSWORD=другой_надежный_пароль_456

# API URL (замените IP)
API_URL=http://ваш_IP:8000
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### 4. Обновление зависимостей

```bash
# Активируйте виртуальное окружение
source venv/bin/activate

# Обновите зависимости
pip install -r requirements.txt
```

### 5. Перезапуск сервисов

```bash
# Перезапустите все сервисы с новым кодом
sudo systemctl restart unified-monitor sentiment-api sentiment-dashboard

# Проверьте статус
sudo systemctl status unified-monitor sentiment-api sentiment-dashboard
```

### 6. Переразметка комментариев

Когда скрипт предложит, запустите переразметку:

```bash
# Используйте screen для надежности
screen -S reclassify

# В screen сессии:
cd ~/dev-monitorshik-test
source venv/bin/activate
python reclassify_all_comments.py

# Отсоединитесь: Ctrl+A, затем D
# Вернитесь: screen -r reclassify
```

**Время выполнения:** 5-15 минут для 200 комментариев

### 7. Проверка результата

Откройте в браузере:
```
http://ваш_IP:8501
```

Введите пароль из `DASHBOARD_PASSWORD` и проверьте:
- ✅ Статистика отображается
- ✅ Комментарии размечены
- ✅ Ручная разметка работает

---

## 🔍 Быстрая диагностика

### Проверка статуса сервисов

```bash
sudo systemctl status unified-monitor sentiment-api sentiment-dashboard
```

Все должны быть **active (running)** ✅

### Проверка API

```bash
curl http://localhost:8000/api/health
```

Должен вернуть JSON с информацией о БД

### Проверка портов

```bash
sudo netstat -tlnp | grep -E '(8000|8501)'
```

Должны быть открыты порты 8000 и 8501

### Просмотр логов

```bash
# Unified monitor (мониторы + анализ тональности)
sudo journalctl -u unified-monitor -f

# API сервер
sudo journalctl -u sentiment-api -f

# Dashboard
sudo journalctl -u sentiment-dashboard -f

# Переразметка
tail -f ~/dev-monitorshik-test/logs/reclassify_*.log
```

Нажмите `Ctrl+C` для выхода из просмотра

---

## ❗ Что делать если возникли проблемы

### 1. API не запускается

```bash
# Проверьте логи
sudo journalctl -u sentiment-api -n 100

# Проверьте .env
nano ~/dev-monitorshik-test/.env

# Попробуйте запустить вручную
cd ~/dev-monitorshik-test
source venv/bin/activate
python api_server.py
```

### 2. Dashboard не работает

```bash
# Проверьте логи
sudo journalctl -u sentiment-dashboard -n 100

# Попробуйте запустить вручную
streamlit run dashboard/streamlit_app.py
```

### 3. Переразметка не работает

```bash
# Проверьте API ключи
python -c "from config.settings import Settings; Settings.load(); print(Settings.YANDEX_API_KEY[:10])"

# Посмотрите логи переразметки
tail -100 ~/dev-monitorshik-test/logs/reclassify_*.log
```

### 4. Восстановление из backup

```bash
cd ~/dev-monitorshik-test
sudo systemctl stop unified-monitor sentiment-api sentiment-dashboard
mv comments.db comments.db.broken
cp comments.db.backup.YYYYMMDD_HHMMSS comments.db
sudo systemctl start unified-monitor sentiment-api sentiment-dashboard
```

---

## 📚 Дополнительная документация

- **QUICK_START_VPS.md** - быстрое руководство (НАЧНИТЕ С НЕГО!)
- **VPS_DEPLOYMENT_STEPS.md** - детальные команды для каждого шага
- **DEPLOYMENT_COMPLETE.md** - информация о подготовленных материалах
- **DEPLOYMENT.md** - общая документация
- **README.md** - описание проекта

---

## ✅ Контрольный чеклист

### Перед развертыванием
- [ ] SSH доступ к VPS работает
- [ ] У вас есть Yandex API ключи
- [ ] Вы знаете IP адрес VPS
- [ ] Файлы загружены на VPS
- [ ] .env файл обновлен с API ключами и паролями

### После развертывания
- [ ] unified-monitor работает (status active)
- [ ] sentiment-api работает (status active)
- [ ] sentiment-dashboard работает (status active)
- [ ] API отвечает на /api/health
- [ ] Dashboard открывается в браузере
- [ ] Переразметка завершена успешно
- [ ] В БД все комментарии имеют тональность
- [ ] Ручная разметка работает
- [ ] Автозапуск включен для всех сервисов
- [ ] Firewall настроен (порты 8000, 8501)

---

## 🎉 Результат

После успешного развертывания у вас будет:

1. ✅ **Unified Monitor** - собирает комментарии из VK и Telegram
2. ✅ **Few-shot анализ тональности** - более точная система Yandex
3. ✅ **Все комментарии переразмечены** - новой системой
4. ✅ **REST API** - `http://ваш_IP:8000` для ручной разметки
5. ✅ **Dashboard** - `http://ваш_IP:8501` для просмотра и разметки
6. ✅ **Автозапуск** - все сервисы запускаются при перезагрузке

---

## 💬 Нужна помощь?

Если застряли на каком-то шаге:

1. Откройте **VPS_DEPLOYMENT_STEPS.md** - там есть troubleshooting
2. Проверьте логи - большинство ошибок видны там
3. Используйте команды из раздела "Быстрая диагностика" выше

---

## 🚀 Начните прямо сейчас!

1. Откройте **QUICK_START_VPS.md**
2. Следуйте инструкциям
3. Через 10-15 минут всё будет работать!

**Удачи! 🍀**

